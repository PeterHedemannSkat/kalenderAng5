import { DeadlineUnit } from '../getDeadlinesFromDates/deadlineUnit';

export class UrlValues {
    constructor (
        public from: string,
        public to: string,
        public txt: string
    ) {}
}
